v0.13.57.2 - Studio Client Data Bridge Hotfix

Built from v0.13.57.1.

Fixes:
- Makes the CRM-to-Studio initialisation handshake reliable even when the embedded iframe loads before the React message listener is active.
- Sends the linked CRM client payload on iframe load for both Instructions Studio and Agreement Studio.
- Retries the Studio-ready handshake until CRM initialisation is acknowledged.
- Refreshes linked client names, family details, adviser details and matter data when an existing draft is opened.
- Replaces any prototype sample client or signatory data accidentally retained in a partial draft with the linked CRM record.
- Advances the service-worker cache and backup source version to v0.13.57.2.

No database, migration or dependency changes from v0.13.57.1.
