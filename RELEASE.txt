v0.13.45 - Adviser Views, Intake Visibility and Document Refinements

- Defaults the dashboard client workload date filter to items due today.
- Shows Contacted intake forms and intake search results across the whole practice, while preserving adviser-scoped export files.
- Defaults the Clients list to the logged-in adviser as primary adviser, with an optional backup-matter toggle and green backup indicator.
- Separates Medical certificate and Chest X-ray checklist items, each with its own expiry date, while carrying legacy Medicals data into Medical certificate.
- Adds current physical address to the full intake questionnaire, internal review, notifications and intake-to-client conversion.

No database migration or new dependency is required.
