v0.13.47 - Dashboard Workload Adviser Scope

- Restricts the dashboard Client workload list to clients where the selected/logged-in adviser is the primary adviser by default.
- Adds a toggle to include matters where that adviser is recorded only as the backup adviser.
- Marks backup-only workload rows with the same green adviser indicator used in the Clients list.
- Removes the dashboard Needs attention and Critical dates side cards.
- Retains Recently viewed and Adviser load beside the workload area.

No database migration, dependency or backend API change is required.
