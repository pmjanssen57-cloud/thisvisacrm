THiS CRM v0.17.30 - Commercial Update Matter Modal Hotfix

Fixes the transparent/overlapping commercial Update matter modal introduced with the unified commercial matter workflow.

The simple Update matter modal now owns its complete visual shell: white background, border, shadow, max-height and overflow behaviour. This keeps the underlying commercial client record from bleeding through the form and keeps the footer/body usable on smaller screens.

Database: unchanged at 45 migrations. No migration added. Migration 45 remains unchanged.
Rollback: restore the v0.17.29 application package; retain all 45 applied migrations.
