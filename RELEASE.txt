v0.13.51 - PWA Install Option Restored

Based on v0.13.50.

Changes:
- Restores the Install app control on the login screen and adviser header whenever the CRM is being viewed in a normal browser.
- The control no longer disappears merely because the browser has not emitted beforeinstallprompt.
- Uses the native browser install prompt when available.
- Shows concise browser-specific installation steps when the automatic prompt is unavailable.
- Continues to hide the control inside the already-installed standalone app.
- Retains the v0.13.50 desktop taskbar icon refinement and existing Android maskable icons.
- Updates the service-worker shell cache to v0.13.51.

No database migration, dependency, authentication or backend API change.
