v0.14.5 - Live Chat Delete Confirmation Layer Fix

- Fixed the delete confirmation for closed chats appearing behind the live-chat slideout.
- Raised the shared CRM confirmation layer above the live-chat drawer, live-chat settings, notifications and other operational popouts.
- The confirmation backdrop now covers the complete CRM interface while the destructive action is awaiting a decision.
- The fix applies to all shared CRM confirmation dialogues, preventing the same layering issue in other slideout workspaces.
- Updated service-worker and embedded asset versions so the corrected styles load after deployment.
- No database migration, API change, dependency change, additional software or subscription is required.
