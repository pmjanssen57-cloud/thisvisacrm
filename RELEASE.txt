v0.13.57.3 - Embedded Studio Initialisation Hotfix

Built from v0.13.57.2.

Fixes:
- Removes the embedded Studios' dependency on browser localStorage/sessionStorage being available inside the CRM iframe.
- Falls back safely to CRM/database persistence when browser storage is blocked.
- Exposes a same-origin direct initialisation hook for both Instructions Studio and Agreement Studio.
- Uses direct iframe initialisation from the CRM with postMessage retained as a fallback.
- Ensures linked client, family, adviser, matter and signatory details replace prototype sample data.
- Restores the A4 preview after linked CRM data is loaded.
- Adds cache-busting version parameters to both Studio iframe URLs.
- Advances the package and service-worker cache to v0.13.57.3.

No database, migration or dependency changes from v0.13.57.2.
