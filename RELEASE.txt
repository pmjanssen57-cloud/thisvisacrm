v0.14.1 - Live Chat Settings Panel Fix

- Fixed Tools > Live chat settings appearing to do nothing.
- Replaced the unstyled settings lightbox wrapper with the CRM shared modal layer, backdrop, card and header components.
- The Tools drawer now closes as the settings panel opens, preventing overlapping drawer and modal layers.
- Preserved all v0.14.0 native live-chat, CRM, Studio, intake, email, signing, print and festive functionality.
- Updated service-worker and embedded Studio asset versions so the corrected interface is fetched after deployment.
- No database migration, API change or dependency change.
