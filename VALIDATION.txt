THiS CRM v0.17.29 validation

- App.jsx and main.jsx pass JSX syntax parsing using the TypeScript JSX parser.
- All Netlify .mjs functions pass node --check.
- All public top-level JavaScript files pass node --check.
- PWA validation script passes.
- PWA service-worker shell cache advances to v0.17.29.
- Database migration count is 45.
- The first 44 database migrations are byte-for-byte unchanged from v0.17.28.
- Migration 45 is additive and only extends commercial_clients with shared matter-workflow fields plus indexes.
- Commercial save/read mapping includes the new workflow fields.
- Commercial records are represented in the unified Clients/My Work layer without moving existing commercial portal/compliance data.
- Commercial My Work saves route back to the original commercial client ID.
- Temporary new-commercial records are replaced by the persisted record after first save.
- Unsaved commercial records cannot use Update matter or Reschedule until the company is saved.
- Deployment ZIP integrity checked after packaging.

A full Vite production build is only reported if node_modules are available in the deployment source package.
