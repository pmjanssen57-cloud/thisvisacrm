THiS CRM v0.16.4 validation

Completed checks
- App.jsx parses successfully as JSX.
- All Netlify .mjs function files pass Node syntax checking.
- netlify/functions/intake.mjs passes Node syntax checking after email/idempotency/upload changes.
- scripts/validate-pwa.mjs passes.
- package.json parses and reports version 0.16.4.
- Runtime/PWA/Studio/live-chat cache references advance to v0.16.4.
- All 41 existing database migration files are byte-for-byte unchanged from v0.16.3.
- No new database migration, dependency or environment variable is introduced.

Assessment reliability covered
- Invalid email formats are blocked before leaving the applicant-details step.
- Server-side email validation rejects invalid formats.
- CV validation occurs when the file is selected.
- Generic browser MIME types are normalised by approved PDF/DOC/DOCX extension.
- Partial CV upload failures retain and reuse the same intake record.
- Repeated initial-save requests with the same per-form submission key return the existing intake record.
- Repeated upload requests for an already stored CV return the existing upload rather than creating another blob.
