THiS CRM v0.13.57.2 validation

Validated:
- Instructions Studio embedded JavaScript syntax passed with node --check.
- Agreement Studio embedded JavaScript syntax passed with node --check.
- CRM and agreement Netlify function syntax passed with node --check.
- Both Studio iframe mounts now initialise on the parent iframe load event.
- Both embedded Studios retry their ready handshake until CRM initialisation is acknowledged.
- Linked CRM client data is reapplied when existing Instruction and Agreement drafts are opened.
- Agreement principal and partner signatory details are refreshed from the linked CRM record.
- Package version, service-worker cache and backup source version are set to v0.13.57.2.
- No database migration or dependency change from v0.13.57.1.

A full Vite production build was not run because the isolated environment does not contain the existing project dependencies and cannot fetch them from the package registry.
