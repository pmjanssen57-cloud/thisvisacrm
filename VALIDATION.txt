THiS CRM v0.14.1 validation

Validated:
- Tools > Live chat settings button closes the Tools drawer and opens the settings modal.
- Live chat settings uses the existing modal layer, backdrop, card, header and action layout classes.
- Settings content remains scrollable within the viewport.
- Save and Close actions remain wired to the existing live-chat settings API.
- v0.14.1 service-worker and Studio asset version references are present.
- JavaScript module syntax checks pass for Netlify and public JavaScript files.
- Package ZIP integrity passes.

No database migration, API contract or dependency change is included.
