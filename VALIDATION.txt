THiS CRM v0.17.5 validation

Completed checks
- Built directly from v0.17.4 My Work Adviser Role Filter.
- App.jsx parses successfully as JSX.
- All Netlify/server .mjs and browser/script .js files pass Node syntax checks.
- Existing PWA validation passes.
- package.json parses and reports version 0.17.5.
- styles.css passes a comment/string-aware structural brace check.
- Quick Move uses the existing saveClient workflow; no separate API or database write path is introduced.
- Quick Move creates a normal matter timeline activity entry for each status change.
- Waiting on INZ cannot be quick-moved without a review date.
- Action/client states cannot be quick-moved without a next action where none already exists.
- Quick Move preserves existing next-action/date information where possible and deliberately leaves matter stage, portal wording and email untouched.
- All 43 existing database migration files are byte-for-byte unchanged from v0.17.4.
- No new migration, dependency or environment variable.

Build environment note
A full Vite production compile was not run because project dependencies are not installed in this sandbox. Netlify deployment remains the final production bundle check.
