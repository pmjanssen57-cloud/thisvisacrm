THiS CRM v0.13.36 validation
============================

Completed in the build workspace:
- package.json JSON validation
- clean dependency installation against the pinned dependency set
- full Vite production build
- App.jsx and main.jsx transformation through Vite
- styles.css production bundling
- Node syntax checks for every Netlify Function
- Node syntax check for the backup recovery script
- verification that node_modules is not included in the release
- verification that automatic Mailchimp API environment keys are not present
- verification that My Day is removed from the CRM tab strip
- verification that the CRM header and mobile navigation can return to My Day

The dependency graph and Yarn-based Netlify deployment configuration are unchanged
from the working v0.13.35 release. The existing large JavaScript chunk warning remains
unchanged and does not prevent a successful production build.
