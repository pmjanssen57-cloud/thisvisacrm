THiS CRM v0.13.57.3 validation

Validated:
- Agreement Studio loads and renders when browser localStorage is unavailable.
- Instructions Studio loads and renders when browser localStorage is unavailable.
- Direct CRM initialisation populates a linked principal applicant, partner, adviser, case type and reference data.
- Agreement Studio replaces prototype client/signatory data and renders seven A4 pages after initialisation.
- Instructions Studio replaces prototype client/family data and renders the linked SMC preview after initialisation.
- Direct same-origin initialisation and postMessage fallback are present for both Studios.
- Embedded Studio JavaScript syntax passed with node --check.
- Netlify function and service-worker JavaScript syntax passed with node --check.
- App.jsx was parsed by TypeScript; only unresolved package/type diagnostics were expected in the dependency-free validation environment.
- Package version and service-worker cache are set to v0.13.57.3.
- No database migration or dependency change from v0.13.57.2.

A full Vite production build was not run because the isolated environment does not contain the existing project dependencies and cannot fetch the exact Netlify packages from the registry.
