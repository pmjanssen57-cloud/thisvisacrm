THiS CRM v0.14.5 validation

Validated:
- App.jsx parses successfully as React JSX using the TypeScript compiler parser.
- Netlify chat and CRM Functions pass Node syntax checks.
- The shared CRM confirmation layer has a z-index above the live-chat overlay and drawer.
- The closed-chat deletion flow continues to require a danger confirmation before the delete API action runs.
- The confirmation backdrop and card remain interactive while the live-chat drawer is open.
- Existing live-chat schedule-aware polling, email notifications, website widget behaviour and client lifecycle controls are unchanged.
- Version references, service-worker cache name and embedded asset query strings are set to v0.14.5.
- PWA validation and ZIP integrity checks pass.

No database migration, API change or dependency change is included.

A complete Vite production build could not be run in this environment because the available package registry does not expose the existing @netlify/blobs@10.7.9 dependency. The dependency declaration is unchanged from the deployed baseline.
