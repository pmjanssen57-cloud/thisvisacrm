THiS CRM v0.16.1 validation

Completed checks
- Built from the v0.16.0 HuB Visual Refresh source package.
- No new database migration added.
- All 41 migration SQL files have SHA-256 hashes identical to v0.16.0.
- All Netlify/server .mjs and browser/script .js files pass `node --check` syntax validation.
- Existing PWA validation script passes.
- package.json parses successfully and reports version 0.16.1.
- App.jsx source structural delimiter counts are balanced and the expected archive actions/workspace are present.
- styles.css passes a structural comment/string-aware brace check.
- Runtime cache/version references have advanced to v0.16.1.
- Diff review confirms changes are limited to the archive feature, related storage/deletion safety, version references and release/guide files.

Environment limitation
A full Vite production compilation was not run in this sandbox because project dependencies are not installed and external npm package installation is unavailable from the container. Netlify deployment will therefore provide the final production-bundle compilation check.

Archive safety design
- The database record is moved to Archived before any linked CV blob is deleted.
- Failed blob cleanup retains the blob key for retry.
- Successful purge metadata clears the live filename/blob key and keeps the original filename separately for archive history and safer rollback behaviour.
- Permanent deletion is admin-only.
