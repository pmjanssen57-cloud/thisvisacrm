THiS CRM v0.17.28 validation

- App.jsx and main.jsx pass JSX syntax parsing using the TypeScript JSX parser.
- All 22 Netlify .mjs functions pass node --check.
- All 6 public top-level JavaScript files pass node --check.
- PWA validation script passes.
- PWA service-worker shell cache advances to v0.17.28.
- 44 database migrations confirmed and byte-for-byte unchanged from v0.17.27.
- Adviser deletion remains restricted to Admin access at the Netlify function boundary.
- Server-side deletion checks protect client, commercial client, intake, agreement, task, calendar, consultation booking/configuration, live-chat and notification-recipient references.
- Self-profile deletion and deletion of the last active Admin adviser are protected.
- Successful delete responses remove the adviser from the current CRM adviser state.
- Deployment ZIP integrity checked after packaging.

A full Vite production build was not run because node_modules are not included in the deployment source package.
