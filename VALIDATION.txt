THiS CRM v0.13.55 validation

Passed
- App.jsx transpiled successfully with the TypeScript JSX parser.
- Generated JavaScript passed node --check.
- Every Netlify .mjs function passed node --check.
- public/sw.js passed node --check.
- PWA validation script passed.
- Top-level function declarations are unique in App.jsx and crm.mjs.
- CSS brace structure passed.
- Static release assertions passed for:
  * authenticated 60-second intake/contact polling;
  * foreground/focus refresh;
  * manual refresh control and last-checked display;
  * lightweight intake-only server endpoint before schema bootstrapping;
  * new-submission detection and CRM toast;
  * missing-CV action and editable email template;
  * assigned-adviser validation, CC and reply-to routing;
  * printable intake view and blocked-popup fallback.

Not run in this isolated environment
- Full dependency-backed Vite production build. Project dependencies are not installed locally and package-registry access was not available. No dependency or build-configuration change is included in this release.
