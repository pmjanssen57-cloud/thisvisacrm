THiS CRM v0.15.7 validation

Validated:
- netlify/functions/chat.mjs JavaScript syntax
- public JavaScript syntax
- App.jsx TypeScript/JSX parse pass (module-resolution warnings ignored)
- HTML parsing for public Studio/chat pages
- PWA validation script
- v0.15.7 runtime/cache-busting references
- package ZIP integrity

Workflow checks:
- chat action creates a Contact-form payload (`formType: contact`) rather than a full assessment payload
- Contact record is assigned to the chat adviser, with current adviser as fallback
- public chat transcript is retained with the Contact record
- existing Contact assessment invitation email/template is reused
- successful questionnaire send marks the Contact record Contacted / Dealt with
- failed email sends leave the Contact record available for retry in Enquiries > Contact forms
- chat footer no longer offers Create enquiry for new conversations

No database migration, dependency or environment-variable changes.
